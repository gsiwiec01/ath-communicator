<Window x:Class="Chat.Clientside.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Chat.Clientside.ViewModel"
        xmlns:local="clr-namespace:Chat.Clientside"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <DockPanel>
            <TextBox Height="20" DockPanel.Dock="Top" />
            <TextBox Height="20" DockPanel.Dock="Top" />
            <Button Height="30" DockPanel.Dock="Top" Content="Połącz z serwerem" Command="{Binding ConnectToServerCommand}"></Button>
            
            <ListView />
        </DockPanel>
        
        <StackPanel Grid.Column="1" >
            <ListView Height="420"></ListView>
            
            <StackPanel Orientation="Horizontal">
                <TextBox Height="30" Width="550"></TextBox>
                <Button Width="50" Content="Wyślij" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
